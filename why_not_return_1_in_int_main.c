#include<stdio.h>
int main(){
    printf("he");
    return 1; // In C, returning 0 from main() indicates successful execution (no errors).
// Returning a non-zero value indicates an error or abnormal termination.
// This value is returned to the operating system or calling process, not the compiler.

// THE USE: It doesn't affect how the program runs, but it can be used by scripts or systems to check for errors.

}